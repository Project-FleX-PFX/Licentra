<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login – Licentra</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="/images/icon.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
</head>
<body class="bg-light">
<div class="container">
  <div class="row justify-content-center align-items-center min-vh-100">
    <div class="col-md-6 col-lg-5">
      <div class="card shadow-lg border-0 rounded-3">
        <div class="card-header bg-white text-center py-4 rounded-top">
          <div class="mb-4">
            <img src="/images/LizentraLogo.png" alt="Licentra Logo" class="img-fluid w-50">
          </div>
          <h3 class="fw-bold text-primary">Licentra Login</h3>
        </div>

        <div class="card-body p-4">
          <% if flash[:notice] %>
            <div class="alert alert-info rounded-3" role="alert">
              <%= flash[:notice] %>
            </div>
          <% end %>

          <% if @error %>
            <div class="alert alert-danger rounded-3" role="alert">
              <%= @error %>
            </div>
          <% end %>

          <form id="login-form" action="/login" method="post">
            <div class="mb-4">
              <label for="email" class="form-label small mb-2">Email</label>
              <input type="email" id="email" name="email" class="form-control py-3 rounded-3" placeholder="Enter email address" required>
            </div>

            <div class="mb-3"> <!-- mb-4 zu mb-3 geändert für etwas weniger Abstand zum neuen Link -->
              <label for="password" class="form-label small mb-2">Password</label>
              <input type="password" id="password" name="password" class="form-control py-3 rounded-3" placeholder="Enter password" required>
            </div>

            <!-- NEUER LINK HINZUGEFÜGT -->
            <div class="mb-4 text-end">
              <a href="/forgot-password" class="small text-primary">Forgot Password?</a>
            </div>
            <!-- ENDE NEUER LINK -->

            <div class="d-flex align-items-center justify-content-center mt-5 mb-0"> <!-- mt-4 zu mt-5 geändert um den vorherigen Abstand beizubehalten -->
              <button type="submit" id="login-button" class="btn btn-primary btn-lg px-5 rounded-3">
                Login
              </button>
            </div>
          </form>
        </div>

        <div class="card-footer text-center py-4 bg-white rounded-bottom">
          <div class="small">
            Don't have an account? <a href="/register" class="fw-bold text-primary">Register here</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
</body>
</html>
